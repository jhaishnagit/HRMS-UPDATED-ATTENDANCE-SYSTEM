Flask==2.3.2
mysql-connector-python==8.0.33
werkzeug==2.3.6
face_recognition==1.3.0
numpy==1.24.3
pandas==1.5.3
geocoder==1.38.1
smtplib==1.0.0  # Note: smtplib is part of Python's standard library, no need to install separately
email-mime-text==0.1.1  # For MIMEText, though smtplib includes this functionality
openpyxl==3.1.2  # Required for Excel export with pandas
pillow==9.5.0  # Required by face_recognition for image processing
dlib==19.24.1  # Dependency for face_recognition (install separately if needed, as it requires compilation)